using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WrestleBookApp : MonoBehaviour
{
    // Start is called before the first frame update
    public WrestleBookView view;
    public WrestleBookController controller;
    public WrestleBookModel model;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
